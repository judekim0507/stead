--- a/chrome/browser/ui/browser_actions.cc
+++ b/chrome/browser/ui/browser_actions.cc
@@ -44,6 +44,8 @@
 #include "chrome/browser/ui/browser_action_id.h"
 #include "chrome/browser/ui/browser_actions.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
+#include "chrome/browser/ui/browser_navigator.h"
+#include "chrome/browser/ui/browser_navigator_params.h"
 #include "chrome/browser/ui/chrome_pages.h"
 #include "chrome/browser/ui/commerce/product_specifications_entry_point_controller.h"
 #include "chrome/browser/ui/commerce/price_tracking_page_action_controller.h"
@@ -52,6 +54,7 @@
 #include "chrome/browser/ui/download/download_bubble_info_utils.h"
 #include "chrome/browser/ui/download/download_bubble_ui_controller.h"
 #include "chrome/browser/ui/download/download_commands.h"
+#include "chrome/common/webui_url_constants.h"
 #include "chrome/browser/ui/find_bar/find_bar.h"
 #include "chrome/browser/ui/find_bar/find_bar_controller.h"
 #include "chrome/browser/ui/global_media_controls/media_toolbar_button_controller.h"
@@ -263,6 +266,32 @@ void BrowserActions::InitializeSidePanelActions() {
                       kActionSidePanelShowBookmarks, bwi, true)
           .Build());
   root_action_item_->AddChild(
+      actions::ActionItem::Builder(
+          base::BindRepeating(
+              [](BrowserWindowInterface* bwi, actions::ActionItem* item,
+                 actions::ActionInvocationContext context) {
+                if (!bwi) {
+                  return;
+                }
+                NavigateParams params(bwi->GetBrowserForMigrationOnly(),
+                                      GURL(chrome::kSteadChatURL),
+                                      WindowOpenDisposition::NEW_FOREGROUND_TAB);
+                Navigate(&params);
+              },
+              bwi))
+          .SetActionId(kActionSidePanelShowReadingList)
+          .SetText(l10n_util::GetStringUTF16(IDS_STEAD_SIDEBAR_TITLE))
+          .SetTooltipText(l10n_util::GetStringUTF16(IDS_STEAD_SIDEBAR_TITLE))
+          .SetImage(ui::ImageModel::FromVectorIcon(
+              vector_icons::kChatIcon, ui::kColorIcon,
+              ui::SimpleMenuModel::kDefaultIconSize))
+          .SetProperty(actions::kActionItemPinnableKey,
+                       std::underlying_type_t<actions::ActionPinnableState>(
+                           actions::ActionPinnableState::kPinnable))
+          .Build());
+  root_action_item_->AddChild(
       SidePanelAction(SidePanelEntryId::kAboutThisSite,
                       IDS_PAGE_INFO_ABOUT_THIS_PAGE_TITLE,
                       IDS_PAGE_INFO_ABOUT_THIS_PAGE_TITLE,
--- a/chrome/app/generated_resources.grd
+++ b/chrome/app/generated_resources.grd
@@ -9270,6 +9270,10 @@
           Reading list
         </message>
       </if>
+      <!-- Stead agent sidebar -->
+      <message name="IDS_STEAD_SIDEBAR_TITLE" desc="Label for the Stead AI sidebar side-panel button (replaces the reading list slot).">
+        Ask Stead
+      </message>
       <message name="IDS_READ_LATER_EXPAND_BUTTON_ARIA_LABEL" desc="Aria label for the Read later list expand button.">
         Expand <ph name="HEADER">$1<ex>Unread</ex></ph>
       </message>
