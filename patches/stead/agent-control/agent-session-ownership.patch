diff --git a/chrome/browser/ui/stead/agent_control/agent_control.mojom b/chrome/browser/ui/stead/agent_control/agent_control.mojom
index e9c7089..578be8b 100644
--- a/chrome/browser/ui/stead/agent_control/agent_control.mojom
+++ b/chrome/browser/ui/stead/agent_control/agent_control.mojom
@@ -113,6 +113,8 @@ struct TabContextInfo {
   int32 tab_id;
   string url;
   string title;
+  bool agent_owned;
+  string owner_session_id;
 };
 
 struct CredentialRef {
@@ -311,7 +313,7 @@ interface AgentControl {
       => (ActionResult result, string? json_result);
 
   Navigate(int32 tab_id, url.mojom.Url url) => (ActionResult result);
-  OpenTab(url.mojom.Url url, bool agent_owned)
+  OpenTab(url.mojom.Url url, bool agent_owned, string owner_session_id)
       => (ActionResult result, int32 tab_id);
   CloseTab(int32 tab_id) => (ActionResult result);
   HandleDialog(string opaque_handle, bool accept, string prompt_text)
diff --git a/chrome/browser/ui/stead/agent_control/stead_agent_control_service.cc b/chrome/browser/ui/stead/agent_control/stead_agent_control_service.cc
index f23c32b..82790d3 100644
--- a/chrome/browser/ui/stead/agent_control/stead_agent_control_service.cc
+++ b/chrome/browser/ui/stead/agent_control/stead_agent_control_service.cc
@@ -1478,6 +1478,7 @@ class SteadAgentControlService::PrivateBrowserControl {
   mojom::ActionResultPtr OpenTab(int32_t action_id,
                                  const GURL& target_url,
                                  bool agent_owned,
+                                 const std::string& owner_session_id,
                                  int32_t* opened_tab_id);
   mojom::ActionResultPtr CloseTab(int32_t tab_id, int32_t action_id);
   mojom::ActionResultPtr HandleDialog(
@@ -1537,6 +1538,7 @@ class SteadAgentControlService::PrivateBrowserControl {
   void EnsureAgentOwnedTabLocked(int32_t tab_id);
   void ReleaseAgentOwnedTab(int32_t tab_id);
   bool IsAgentOwnedTab(int32_t tab_id) const;
+  std::string AgentOwnerSession(int32_t tab_id) const;
   mojom::ActionClass ClassifyAccessibilityAction(
       const mojom::NodeRef& ref,
       ax::mojom::Action action,
@@ -1689,6 +1691,7 @@ class SteadAgentControlService::PrivateBrowserControl {
   base::flat_map<int32_t, std::unique_ptr<ObservedTab>> observed_tabs_;
   base::flat_map<int32_t, std::unique_ptr<SnapshotIndex>> snapshot_indexes_;
   base::flat_set<int32_t> agent_owned_tabs_;
+  base::flat_map<int32_t, std::string> agent_owner_sessions_;
   base::flat_map<int32_t, content::WebContents::ScopedIgnoreInputEvents>
       agent_input_blocks_;
   base::WeakPtrFactory<PrivateBrowserControl> weak_factory_{this};
@@ -2226,6 +2229,8 @@ void SteadAgentControlService::PrivateBrowserControl::Shutdown() {
   snapshot_generations_.clear();
   scoped_ax_modes_.clear();
   agent_owned_tabs_.clear();
+  agent_owner_sessions_.clear();
+  agent_input_blocks_.clear();
 }
 
 SteadAgentControlService::SteadAgentControlService(Profile* profile)
@@ -2281,6 +2286,16 @@ void SteadAgentControlService::BindConsole(
   console_receivers_.Add(this, std::move(receiver));
 }
 
+bool SteadAgentControlService::IsAgentOwnedTabForUi(int32_t tab_id) const {
+  return browser_control_ && browser_control_->IsAgentOwnedTab(tab_id);
+}
+
+std::string SteadAgentControlService::AgentOwnerSessionForUi(
+    int32_t tab_id) const {
+  return browser_control_ ? browser_control_->AgentOwnerSession(tab_id)
+                          : std::string();
+}
+
 void SteadAgentControlService::Observe(
     mojo::PendingRemote<mojom::ControlObserver> observer) {
   observers_.Add(std::move(observer));
@@ -2385,6 +2400,9 @@ void SteadAgentControlService::GetActiveTabContext(
   context->tab_id = session_id.id();
   context->url = url.spec();
   context->title = base::UTF16ToUTF8(contents->GetTitle());
+  context->agent_owned = browser_control_->IsAgentOwnedTab(session_id.id());
+  context->owner_session_id =
+      browser_control_->AgentOwnerSession(session_id.id());
   std::move(callback).Run(std::move(context));
 }
 
@@ -2416,6 +2434,9 @@ void SteadAgentControlService::ListOpenTabContexts(
       context->tab_id = session_id.id();
       context->url = url.spec();
       context->title = base::UTF16ToUTF8(contents->GetTitle());
+      context->agent_owned = browser_control_->IsAgentOwnedTab(session_id.id());
+      context->owner_session_id =
+          browser_control_->AgentOwnerSession(session_id.id());
       contexts.push_back(std::move(context));
     }
   }
@@ -2947,6 +2968,7 @@ mojom::ActionResultPtr SteadAgentControlService::PrivateBrowserControl::OpenTab(
     int32_t action_id,
     const GURL& target_url,
     bool agent_owned,
+    const std::string& owner_session_id,
     int32_t* opened_tab_id) {
   if (opened_tab_id) {
     *opened_tab_id = 0;
@@ -2962,7 +2984,9 @@ mojom::ActionResultPtr SteadAgentControlService::PrivateBrowserControl::OpenTab(
   }
 
   content::OpenURLParams params(target_url, content::Referrer(),
-                                WindowOpenDisposition::NEW_FOREGROUND_TAB,
+                                agent_owned
+                                    ? WindowOpenDisposition::NEW_BACKGROUND_TAB
+                                    : WindowOpenDisposition::NEW_FOREGROUND_TAB,
                                 ui::PAGE_TRANSITION_TYPED,
                                 /*is_renderer_initiated=*/false);
   content::WebContents* opened = window->OpenURL(
@@ -2992,18 +3016,39 @@ mojom::ActionResultPtr SteadAgentControlService::PrivateBrowserControl::OpenTab(
   TabStripModel* tab_strip = window->GetTabStripModel();
   if (agent_owned) {
     agent_owned_tabs_.insert(tab_id.id());
-    if (tab_strip && tab_strip->SupportsTabGroups()) {
+    agent_owner_sessions_[tab_id.id()] = owner_session_id;
+    agent_input_blocks_.insert_or_assign(
+        tab_id.id(), opened->IgnoreInputEvents(std::nullopt));
+    if (tab_strip) {
       const int index = tab_strip->GetIndexOfWebContents(opened);
       if (index != TabStripModel::kNoTab) {
-        const tab_groups::TabGroupId group_id =
-            tab_strip->AddToNewGroup({index});
-        tab_strip->ChangeTabGroupVisuals(
-            group_id, tab_groups::TabGroupVisualData(
-                          kAgentTabGroupTitle,
-                          tab_groups::TabGroupColorId::kCyan));
+        if (tab_strip->SupportsTabGroups()) {
+          std::optional<tab_groups::TabGroupId> agent_group_id;
+          for (const tab_groups::TabGroupId& group_id :
+               tab_strip->group_model()->ListTabGroups()) {
+            const TabGroup* group =
+                tab_strip->group_model()->GetTabGroup(group_id);
+            if (group &&
+                group->visual_data()->title() == kAgentTabGroupTitle) {
+              agent_group_id = group_id;
+              break;
+            }
+          }
+          if (agent_group_id) {
+            tab_strip->AddToExistingGroup({index}, *agent_group_id);
+          } else {
+            const tab_groups::TabGroupId group_id =
+                tab_strip->AddToNewGroup({index});
+            tab_strip->ChangeTabGroupVisuals(
+                group_id, tab_groups::TabGroupVisualData(
+                              kAgentTabGroupTitle,
+                              tab_groups::TabGroupColorId::kCyan));
+          }
+        }
+        // Activate only after ownership and input blockade are installed so
+        // tab-selection observers can open the owning thread immediately.
+        tab_strip->ActivateTabAt(index);
       }
-      agent_input_blocks_.insert_or_assign(tab_id.id(),
-                                           opened->IgnoreInputEvents(std::nullopt));
     }
   }
 
@@ -4110,6 +4140,7 @@ void SteadAgentControlService::Navigate(int32_t tab_id,
 
 void SteadAgentControlService::OpenTab(const GURL& target_url,
                                        bool agent_owned,
+                                       const std::string& owner_session_id,
                                        OpenTabCallback callback) {
   const int32_t action_id = NextActionId();
   const bool is_file_access = target_url.is_valid() &&
@@ -4154,7 +4185,7 @@ void SteadAgentControlService::OpenTab(const GURL& target_url,
 
   int32_t opened_tab_id = 0;
   mojom::ActionResultPtr result = browser_control_->OpenTab(
-      action_id, target_url, agent_owned, &opened_tab_id);
+      action_id, target_url, agent_owned, owner_session_id, &opened_tab_id);
   if (result->ok && opened_tab_id != 0) {
     pending_action_by_tab_[opened_tab_id] = action_id;
     SchedulePendingActionClear(opened_tab_id, action_id);
@@ -5315,6 +5346,7 @@ void SteadAgentControlService::PrivateBrowserControl::ClearTabState(
   snapshot_indexes_.erase(tab_id);
   scoped_ax_modes_.erase(tab_id);
   agent_owned_tabs_.erase(tab_id);
+  agent_owner_sessions_.erase(tab_id);
   agent_input_blocks_.erase(tab_id);
 }
 
@@ -5334,6 +5366,13 @@ bool SteadAgentControlService::PrivateBrowserControl::IsAgentOwnedTab(
   return agent_owned_tabs_.contains(tab_id);
 }
 
+std::string
+SteadAgentControlService::PrivateBrowserControl::AgentOwnerSession(
+    int32_t tab_id) const {
+  const auto owner = agent_owner_sessions_.find(tab_id);
+  return owner == agent_owner_sessions_.end() ? std::string() : owner->second;
+}
+
 void SteadAgentControlService::PrivateBrowserControl::EnsureAgentOwnedTabLocked(
     int32_t tab_id) {
   if (IsAgentOwnedTab(tab_id)) {
@@ -5346,6 +5385,7 @@ void SteadAgentControlService::PrivateBrowserControl::ReleaseAgentOwnedTab(
   if (!agent_owned_tabs_.erase(tab_id)) {
     return;
   }
+  agent_owner_sessions_.erase(tab_id);
   agent_input_blocks_.erase(tab_id);
   content::WebContents* contents = FindTabById(tab_id);
   if (!contents) {
diff --git a/chrome/browser/ui/stead/agent_control/stead_agent_control_service.h b/chrome/browser/ui/stead/agent_control/stead_agent_control_service.h
index cc1ee57..6ed08dc 100644
--- a/chrome/browser/ui/stead/agent_control/stead_agent_control_service.h
+++ b/chrome/browser/ui/stead/agent_control/stead_agent_control_service.h
@@ -82,6 +82,9 @@ class SteadAgentControlService : public KeyedService,
   void Bind(mojo::PendingReceiver<mojom::AgentControl> receiver);
   void BindConsole(mojo::PendingReceiver<mojom::ControlConsole> receiver);
 
+  bool IsAgentOwnedTabForUi(int32_t tab_id) const;
+  std::string AgentOwnerSessionForUi(int32_t tab_id) const;
+
   // mojom::AgentControl:
   void Observe(mojo::PendingRemote<mojom::ControlObserver> observer) override;
   void ListTabs(ListTabsCallback callback) override;
@@ -100,6 +103,7 @@ class SteadAgentControlService : public KeyedService,
                 NavigateCallback callback) override;
   void OpenTab(const GURL& url,
                bool agent_owned,
+               const std::string& owner_session_id,
                OpenTabCallback callback) override;
   void CloseTab(int32_t tab_id, CloseTabCallback callback) override;
   void HandleDialog(const std::string& handle,
diff --git a/chrome/browser/ui/stead/brain/stead_brain_service.cc b/chrome/browser/ui/stead/brain/stead_brain_service.cc
index f665216..af65fe4 100644
--- a/chrome/browser/ui/stead/brain/stead_brain_service.cc
+++ b/chrome/browser/ui/stead/brain/stead_brain_service.cc
@@ -898,7 +898,7 @@ void SteadBrainService::HandleToolCall(const base::DictValue& event) {
     const bool agent_owned =
         arguments.FindBool("agent_owned").value_or(true);
     control->OpenTab(
-        GURL(*url), agent_owned,
+        GURL(*url), agent_owned, session_id,
         base::BindOnce(
             [](base::WeakPtr<SteadBrainService> service,
                std::string session_id, std::string tool_call_id,
diff --git a/chrome/browser/ui/views/side_panel/reading_list/reading_list_side_panel_coordinator.cc b/chrome/browser/ui/views/side_panel/reading_list/reading_list_side_panel_coordinator.cc
--- a/chrome/browser/ui/views/side_panel/reading_list/reading_list_side_panel_coordinator.cc
+++ b/chrome/browser/ui/views/side_panel/reading_list/reading_list_side_panel_coordinator.cc
@@ -14,4 +14,6 @@
 #include "chrome/browser/ui/side_panel/side_panel_registry.h"
 #include "chrome/browser/ui/side_panel/side_panel_ui.h"
+#include "chrome/browser/ui/stead/agent_control/stead_agent_control_service.h"
+#include "chrome/browser/ui/stead/agent_control/stead_agent_control_service_factory.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "components/sessions/content/session_tab_helper.h"
@@ -72,12 +74,20 @@ void ReadingListSidePanelCoordinator::OnTabStripModelChanged(
   }
   auto next = open_by_tab_.end();
+  bool agent_owned = false;
   if (selection.new_contents) {
     const SessionID new_id =
         sessions::SessionTabHelper::IdForTab(selection.new_contents);
     if (new_id.is_valid()) {
       next = open_by_tab_.find(new_id.id());
+      auto* control = stead::SteadAgentControlServiceFactory::GetForProfile(
+          &profile_.get());
+      agent_owned = control && control->IsAgentOwnedTabForUi(new_id.id());
+      if (agent_owned) {
+        open_by_tab_[new_id.id()] = true;
+        next = open_by_tab_.find(new_id.id());
+      }
     }
   }
   const bool should_show =
-      next != open_by_tab_.end() && next->second;
+      agent_owned || (next != open_by_tab_.end() && next->second);
   const bool is_showing = side_panel_ui->IsSidePanelEntryShowing(key);
diff --git a/chrome/browser/ui/views/side_panel/BUILD.gn b/chrome/browser/ui/views/side_panel/BUILD.gn
--- a/chrome/browser/ui/views/side_panel/BUILD.gn
+++ b/chrome/browser/ui/views/side_panel/BUILD.gn
@@ -128,4 +128,5 @@ source_set("side_panel") {
     "//chrome/browser/ui/extensions",
     "//chrome/browser/ui/side_panel",
+    "//chrome/browser/ui/stead/agent_control",
     "//chrome/browser/ui/tabs:tab_strip",
     "//chrome/browser/ui/tabs:tabs_public",
diff --git a/content/browser/web_contents/web_contents_impl.cc b/content/browser/web_contents/web_contents_impl.cc
--- a/content/browser/web_contents/web_contents_impl.cc
+++ b/content/browser/web_contents/web_contents_impl.cc
@@ -3041,7 +3041,6 @@ WebContents::ScopedIgnoreInputEvents WebContentsImpl::IgnoreInputEvents(
     } while (web_input_event_audit_callbacks_.contains(callback_id));
     web_input_event_audit_callbacks_[callback_id] = std::move(*audit_callback);
   } else {
-#if BUILDFLAG(IS_ANDROID)
     if (ignore_input_events_count_ == 0) {
       // Reset gesture detection before starting input suppression so that any
       // ongoing scroll gesture is correctly finished.
@@ -3052,7 +3051,6 @@ WebContents::ScopedIgnoreInputEvents WebContentsImpl::IgnoreInputEvents(
         static_cast<RenderWidgetHostViewBase*>(view)->ResetGestureDetection();
       }
     }
-#endif
     ++ignore_input_events_count_;
   }
 
@@ -3068,7 +3066,6 @@ WebContents::ScopedIgnoreInputEvents WebContentsImpl::IgnoreInputEvents(
             CHECK(it != wc->web_input_event_audit_callbacks_.end());
             wc->web_input_event_audit_callbacks_.erase(it);
           } else {
-#if BUILDFLAG(IS_ANDROID)
             // Reset gesture detection so that we don't continue to generate new
             // gestures from suppressed touches. These suppressed gestures would
             // otherwise confuse the event stream validator when input is
@@ -3083,7 +3080,6 @@ WebContents::ScopedIgnoreInputEvents WebContentsImpl::IgnoreInputEvents(
                     ->ResetGestureDetection();
               }
             }
-#endif
             --wc->ignore_input_events_count_;
           }
         }
